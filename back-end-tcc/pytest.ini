[pytest]
testpaths = tests
markers =
    unit: isolated tests with mocked dependencies
    integration: tests that exercise HTTP endpoints with a real test database
    behavior: business behavior scenarios written in Given-When-Then form
    load: concurrent load smoke tests with response-time thresholds

